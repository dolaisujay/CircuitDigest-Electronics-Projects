// ESP32-CAM Security Motion Detector
#include "esp_camera.h"
#include <WiFi.h>

void setup() {
  Serial.begin(115200);
  // Camera initialization code
}

void loop() {
  // Motion detection loop
  delay(1000);
}
